server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mariavanesalopez/patrones-spring-cloud.git
#          default-label: master
#          search-paths: caja

